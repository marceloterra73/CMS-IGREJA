/**
 * MÓDULOS ESPECÍFICOS PARA IGREJAS — ARQUITETURA MODULAR (FASE 3)
 * Definições e contratos conceituais dos módulos eclesiais.
 * Base puramente declarativa e estrutural para o projeto.
 *
 * Proibido implementar:
 * isModuleEnabled(), enableModule(), disableModule(), loadModule(), registerModule()
 * ou qualquer outro mecanismo operacional equivalente.
 */

export * from '../types';
export { CHURCH_MODULES_CATALOG } from '../constants';



